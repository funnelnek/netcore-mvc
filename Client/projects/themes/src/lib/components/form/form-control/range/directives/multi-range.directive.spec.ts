import { MultiRangeDirective } from './multi-range.directive';

describe('MultiRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiRangeDirective();
    expect(directive).toBeTruthy();
  });
});
