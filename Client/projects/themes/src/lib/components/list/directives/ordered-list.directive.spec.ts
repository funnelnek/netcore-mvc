import { OrderedListDirective } from './ordered-list.directive';

describe('OrderedListDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderedListDirective();
    expect(directive).toBeTruthy();
  });
});
